# 凭据与安全

> 版本: v1.1 | 2026-06-17 创建
> 核心原则：**凭据明文只存于本地"私密文档"**（不上传 GitHub），Git 仓库内容**绝不包含任何明文凭据**。

---

## 1. 信任边界（必须先想清楚）

| 区域 | 信任等级 | 是否可写明文 | 说明 |
|------|---------|------------|------|
| `02_凭据与安全.md`（**本文档**） | 🔴 **私密** | ✅ 可以 | 已在 `.gitignore` 排除，**绝不会 push 到 GitHub** |
| Git 仓库内任何其他文件 | 🟢 **公开**（即使仓库私有也可能在日志/截图/分享时泄露） | ❌ **绝对不能** | README/脚本/.env.example 都属此类 |
| OpenClaw 配置文件 | 🟡 **VM 本地** | ✅ 可以 | `/home/wwh/.openclaw/openclaw.json` 在 VM 内部 |
| 手机 QQ 消息 | 🟡 **私聊** | ✅ 可以 | 私聊 1v1 不会外泄 |

---

## 2. GitHub Personal Access Token

| 项 | 值 |
| --- | --- |
| **类型** | Fine-grained personal access token |
| **Token ID** | `11APKHNJI0`（前 10 位） |
| **授权范围** | Only `WWH3602/second-brain` 仓库 |
| **权限** | Contents: Read and write |
| **过期时间** | 90 天（约 2026-09-15 需轮换） |

### 明文 Token（已记录于此，**本文件不上传 GitHub**）

```
GITHUB_TOKEN=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
```

> ⚠️ **本文件受 `.gitignore` 保护**（已在 `03_第二大脑/.gitignore` 排除 `02_凭据与安全.md`），绝不会 push 到 GitHub。

### 新对话的 AI 如何连上仓库？

**AI 拿到本文件后，按以下流程操作**：

1. **读取本文件** → 得到明文 Token
2. **在本地 shell 临时导出** → `export GITHUB_TOKEN=<明文>`
3. **配 git remote** → `git remote set-url origin https://oauth2:${GITHUB_TOKEN}@github.com/WWH3602/second-brain.git`
4. **正常 pull / push** → 凭据在 git 内部使用，不会出现在命令历史（用临时环境变量）
5. **完成后** → `unset GITHUB_TOKEN`

> 关键：**AI 不会把明文写进任何新文件**，只是临时在 shell 里用一次。

---

## 3. OpenClaw 服务器连接（VM 上已部署的耗子助手）

### 3.1 SSH 连接
| 项 | 值 |
| --- | --- |
| **外网域名** | `yidao.picp.net` |
| **SSH 端口** | `10022` |
| **内网直连** | `ssh wwh@192.168.3.112` |
| **用户名** | `wwh` |
| **密码** | `yd@12345` |
| **Home 目录** | `/home/wwh` |
| **免密登录** | ✅ 已配置（公钥已上传） |

### 3.2 OpenClaw Web 访问
| 项 | 值 |
| --- | --- |
| **Gateway 端口** | `18789` |
| **访问 Token** | `b959248134d0ebbd145a2b0123716a87e42498440198dd82` |
| **完整 URL（本地隧道）** | `http://127.0.0.1:18789/#token=b959248134d0ebbd145a2b0123716a87e42498440198dd82` |

### 3.3 快速连接命令
```powershell
# 1. 建立 SSH 隧道（后台）
ssh -L 18789:127.0.0.1:18789 wwh@yidao.picp.net -p 10022 -N

# 2. 浏览器打开
http://127.0.0.1:18789/#token=b959248134d0ebbd145a2b0123716a87e42498440198dd82
```

---

## 4. AI 配置文件位置

| 文件 | 路径 | 说明 |
|------|------|------|
| **OpenClaw 主配置** | `~/.openclaw/openclaw.json` | 模型/provider/服务配置 |
| **OpenClaw hooks** | `~/.openclaw/hooks/session-memory/` | 会话记忆 hook |
| **QQ 机器人配置** | `~/.openclaw/channels/qqbot.json` | QQ 频道配置 |
| **本仓库** | `~/second-brain/`（待 clone） | 第二大脑 Git 仓库 |

---

## 5. 轮换策略

### GitHub PAT
- 到期前 7 天提前生成新 Token
- 旧 Token 立刻在 GitHub 端 **Revoke**
- 更新本文件的"明文 Token"段
- 同步更新 OpenClaw 端 `~/.bashrc` 或 `openclaw.json` 里的引用
- 验证 push/pull 正常

### 应急响应（如怀疑泄露）
1. 立刻在 GitHub → Settings → Developer settings → Personal access tokens → 找到 `11APKHNJI0` → **Revoke**
2. 重新生成新 Token，更新本文档
3. 检查 `second-brain` 仓库的 commit 历史，确认没有异常 push
4. 在 `memory_events.jsonl` 记录 `type: correction` 事件

---

**最后更新**: 2026-06-17（v1.1：补充信任边界设计 + 新对话 AI 连接流程）
